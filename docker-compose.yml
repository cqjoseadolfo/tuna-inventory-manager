version: '3.8'
services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:vinext -- -p 3001 -H 0.0.0.0"
    environment:
      - NODE_ENV=development
